{:itonami.blueprint/id "cloud-itonami-isic-6492"
 :itonami.blueprint/name "Other credit granting"
 :itonami.blueprint/isic-rev5 "6492"
 :itonami.blueprint/domain :finance/credit-granting
 :itonami.blueprint/license "AGPL-3.0-or-later"
 :itonami.blueprint/social-impact [:financial-inclusion :data-sovereignty :transparency]
 :itonami.blueprint/governor :credit-governor
 :itonami.blueprint/required-technologies [:robotics :identity :forms :dmn :bpmn :audit-ledger :banking]
 :itonami.blueprint/optional-technologies []
 :itonami.blueprint/status :public-oss
 :itonami.blueprint/robotics true
 :itonami.blueprint/maturity :implemented
 ;; Deployed as a Cloudflare PAGES project (pages_build_output_dir), which is
 ;; why an earlier check with `wrangler deployments list` — a Workers-only
 ;; command — reported this actor as not deployed. `wrangler pages project
 ;; list` shows it live.
 ;; Pages Functions under /api/loan/*; /health is the SPA index, not an API.
 ;; No :health-path is declared, deliberately: there is no health endpoint to
 ;; name, and naming /health would make a probe report the SPA index as a
 ;; healthy API.
 :itonami.blueprint/endpoint "https://cloud-itonami-isic-6492.pages.dev"
 :itonami.blueprint/endpoint-kind :pages-dev}
