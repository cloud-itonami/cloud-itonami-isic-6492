{:paths ["src"]
 ;; langgraph-clj brings langchain-clj transitively (both zero-/single-dep,
 ;; all .cljc -> portable to JVM / SCI / ClojureScript / GraalVM). It lives
 ;; in the com-junkawasaki org of this workspace. This repo is standalone
 ;; (forkable outside the workspace); the :local/root paths only resolve
 ;; inside the monorepo checkout — a standalone fork should override them
 ;; with git coordinates.
 :deps {
        ;; kotoba.lang.error replaced clojure.lang.ExceptionInfo here (2026-09-25).
        ;; An explicit floor: tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/error {:git/sha "838dec56c9a5b74f9eec825a2fb28bda157f8cd3"}
        ;; kotoba.io replaced clojure.java.io here (2026-09-24). An explicit floor:
        ;; kotoba.io carries clojure.java.io's file/as-file/as-relative-path/
        ;; make-parents/delete-file from 98045c7, and tools.deps takes the NEWEST
        ;; sha it is shown.
        io.github.kotoba-lang/io {:git/sha "72dde04b4c32854d70d17a670b277f8f7957a784"}
        ;; kotoba.pprint replaced clojure.pprint here (2026-09-24): the same
        ;; bytes (cljs.pprint transcribed). An explicit floor; tools.deps takes the
        ;; NEWEST sha it is shown.
        io.github.kotoba-lang/pprint {:git/sha "5c9d4aea71d6caac529fffcf361aba0f87ee485e"}
        ;; kotoba.lang.coll replaced clojure.set here (2026-09-24). An explicit
        ;; floor: kotoba.lang.coll carries clojure.set's union/intersection/
        ;; difference/map-invert from 0ffd5f6, and tools.deps takes the NEWEST
        ;; sha it is shown.
        io.github.kotoba-lang/coll {:git/sha "f74e248f0a07e8ae572885f50fab6bdb39dc24be"}
        ;; kotoba.lang.edn replaced clojure.edn here (2026-09-23). An explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, and kbb resolves git deps
        ;; only from nbb.edn, so every project file this repo has names it.
        io.github.kotoba-lang/edn {:git/sha "79dba32c8892058842c57783f06a2dfe4601aeb0"}
       ;; kotoba.test (clojure.test-compatible surface), not clojure.test --
        ;; scripts/migrate-clojure-test-to-kotoba-test.cljk. Explicit floor.
        io.github.kotoba-lang/test {:git/url "https://github.com/kotoba-lang/test.git"
                                    :git/sha "2ab4885f99fd25552d380d0f6b0b54c5a72eb7ae"}
        ;; kotoba.robotics.process: the process-physics solvers and probe this
        ;; repo's physics.edn runs on (physical-AI bot, scripts/physical-ai-bots).
        io.github.kotoba-lang/robotics {:local/root "../../kotoba-lang/robotics"}

        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       io.github.com-junkawasaki/langgraph-clj
        {:local/root "../../kotoba-lang/langgraph"}
        io.github.kotoba-lang/langchain-store
        {:local/root "../../kotoba-lang/langchain-store"}
        ;; The KIR interpreter is a RUNTIME dependency, and it is not an
        ;; implementation detail: it is what makes
        ;; `src/credit/kernels/gate.kotoba` the thing that DECIDES.
        ;; `credit.kernels.kotoba-oracle` executes the shipped
        ;; `credit.kernels.gate-kir` through it, so the governor's verdict
        ;; and the phase gate are what the `.kotoba` says rather than what a
        ;; second copy in `.cljc` said. Portable `.cljc` -- it compiles and
        ;; runs under `cljs.main --target node` (measured), which it has to,
        ;; because that and the Cloudflare Worker are where this actor
        ;; actually decides.
        ;;
        ;; The COMPILER stays test-only (see :test): it produced the
        ;; artifact and never reaches a consumer.
        ;;
        ;; Pinned to the kir the pinned compiler emits for --
        ;; `io.github.kotoba-lang/amu` 806f5cef declares kotoba-kir
        ;; d58972da. They have to match: KIR is a versioned document, and an
        ;; interpreter reading a shape its emitter did not write is exactly
        ;; the failure the artifact was supposed to remove.
        io.github.kotoba-lang/osaho
        {:git/url "https://github.com/kotoba-lang/osaho.git"
         :git/sha "d58972da61758584a5bec0e863bcb3ea6fdd6c64"}}
 :aliases
 {
  ;; kbb -M:physics -> the physical-AI bot's measurement: runs physics.edn on
  ;; kotoba.robotics.process and prints one probe map. Exit 2 = could not measure.
  :physics {:main-opts ["-m" "kotoba.robotics.process.probe"]}
  ;; kbb -M:physai-test -> test/'s .cljk on kbb's runner (the bot's test count;
  ;; a .kotoba vertical's own :test may refuse kbb on purpose).
  ;; -n: the portable namespaces credit.portable-cljs-test-runner runs, plus the physics test.
  ;; test/wasm/ and test/credit/kernels/{gate_kotoba*,kotoba_oracle_test} host .wasm through
  ;; kototama.tender (Chicory) -- JVM-only by design, run by the :test alias.
  :physai-test {:extra-paths ["test" "test-physai"]
                :main-opts ["-m" "cognitect.test-runner" "-d" "test/credit" "-d" "test-physai"
                            "-n" "credit.facts-test" "-n" "credit.governor-contract-test" "-n" "credit.kernels.gate-test" "-n" "credit.kernels.kotoba-oracle-portable-test" "-n" "credit.phase-test" "-n" "credit.registry-test" "-n" "credit.edge.auth-test" "-n" "credit.edge.caller-allowlist-test" "-n" "credit.edge.kotobase-store-test" "-n" "credit.edge.kv-store-test" "-n" "credit.edge.loan-endpoints-test" "-n" "credit.store-contract-test" "-n" "credit.store-numeric-identity-test" "-n" "credit.physics-spec-test"]}
;; offline workspace dev -- pin transitive langchain-clj to local checkout.
  ;; TWO distinct maven coordinates resolve to this same physical repo in
  ;; this dep graph: io.github.com-junkawasaki/langchain-clj AND
  ;; io.github.kotoba-lang/langchain (langgraph-clj's OWN declared
  ;; coordinate, git-SHA-pinned) -- see the identical fix + full
  ;; reasoning in cloud-itonami-commitment-ledger's deps.edn (same
  ;; migration, same fleet, same dep graph shape). Without overriding
  ;; BOTH, `kbb -M:dev:test` silently loads a STALE gitlibs-pinned
  ;; `langchain/kotoba_db.cljc` predating `kotoba-api-async` instead of
  ;; this local checkout's copy.
  :dev {:override-deps
        {
         io.github.kotoba-lang/langchain
         {:local/root "../../kotoba-lang/langchain"}}}
  ;; kbb -M:dev:run  -> drive the demo through one OperationActor
  :run {:main-opts ["-m" "credit.sim"]}
  ;; kbb -M:dev:render-html [out-file] -> regenerate
  ;; docs/samples/operator-console.html by driving the REAL actor
  ;; (credit.operation -> credit.governor -> credit.store) and rendering
  ;; what that run produced. Flagship checklist item 2, ADR-2607189300.
  ;;
  ;; jp-go-digital-design-system (DADS) supplies the console's CSS:
  ;; `jp-go-dds.skin/dds+skin` is the compat skin (vendored dds.css then
  ;; the skin rules) so the console's plain semantic markup picks up the
  ;; workspace design system instead of hand-rolling a second one. It is
  ;; scoped to THIS alias -- a build-time renderer dependency that no
  ;; runtime consumer of this actor ever loads. Pinned by git sha, the
  ;; same sha the sibling consoles in this fleet pin.
  :render-html {:extra-deps {io.github.kotoba-lang/jp-go-digital-design-system
                             {:git/url "https://github.com/kotoba-lang/jp-go-digital-design-system"
                              :git/sha "83db99546eabc53613ae826ea41d1533a3c574af"}}
                :main-opts ["-m" "credit.render-html"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}
                      ;; kototama.tender hosts the wasm/affordability.kotoba
                      ;; build (see wasm/README.md) -- test-only, the actor's
                      ;; own runtime code has no wasm/host dependency.
                      io.github.kotoba-lang/kototama
                      {:local/root "../../kotoba-lang/kototama"}
                      ;; Produces `src/credit/kernels/gate_kir.cljk` (see
                      ;; the :gen alias) and re-produces it in
                      ;; `credit.kernels.kotoba-oracle-test` to prove what
                      ;; shipped is what the source says. Test-only on
                      ;; purpose: a consumer runs the artifact, never the
                      ;; compiler. Pin and the runtime kotoba-kir pin move
                      ;; TOGETHER -- see the :deps comment.
                      io.github.kotoba-lang/amu
                      {:git/url "https://github.com/kotoba-lang/amu.git"
                       :git/sha "806f5cef7d5bc11ad8ec8514ad6121efc73be6c7"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; Regenerate the shipped decision core. Combine with :test, which is
  ;; where the compiler lives:
  ;;
  ;;   kbb -M:dev:test:gen
  :gen {:main-opts ["-m" "credit.kernels.kotoba-oracle-gen"]}
  ;; PRIMARY gate: the portable .cljc suite under a real ClojureScript
  ;; host (runtime priority: cljs first, JVM compat). The JVM-only
  ;; wasm.affordability-test (kototama.tender/Chicory) is excluded --
  ;; see credit.portable-cljs-test-runner's docstring.
  ;;   kbb -Sdeps '{:paths ["src" "test"]}' -M:dev:cljs \
  ;;     -m cljs.main --target node -m credit.portable-cljs-test-runner
  ;; Also supplies shadow-cljs.edn's :edge-api build (docs/adr/0002-http-
  ;; edge-loan-intake.md) -- same pattern cloud-itonami-commitment-
  ;; ledger's own deps.edn :cljs alias uses.
  :cljs {:extra-deps {org.clojure/clojurescript {:mvn/version "1.11.132"}
                      thheller/shadow-cljs {:mvn/version "2.28.20"}}}
  ;; static analysis -- clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
